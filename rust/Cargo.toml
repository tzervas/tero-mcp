[package]
name = "tero-mcp"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "tero-mcp"
path = "src/bin/tero-mcp.rs"

[dependencies]
# Note: for full port, dependencies from mycelium-tero would be here.
# For now, this is placeholder; full port would extract the model etc.
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Add other as needed from port
